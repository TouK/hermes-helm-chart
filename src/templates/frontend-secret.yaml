apiVersion: v1
kind: Secret
metadata:
  name: {{ include "hermes.frontend.fullname" . }}-secret
data: 
  frontend-secret.properties: |-
  {{ include "print.secrets" .Values.frontend | b64enc | indent 2}}